<%= form_with(model: cadastre, local: true) do |form| %>
  <% if cadastre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cadastre.errors.count, "error") %> prohibited this cadastre from being saved:</h2>

      <ul>
      <% cadastre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :cpf %>
    <%= form.text_field :cpf %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
