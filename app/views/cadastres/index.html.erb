<p id="notice"><%= notice %></p>

<h1>Cadastres</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Cpf</th>
      <th>Phone</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cadastres.each do |cadastre| %>
      <tr>
        <td><%= cadastre.name %></td>
        <td><%= cadastre.cpf %></td>
        <td><%= cadastre.phone %></td>
        <td><%= link_to 'Show', cadastre %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo Cadastro', new_cadastre_path %>
<%= link_to 'Estatisticas', statistics_path %>
